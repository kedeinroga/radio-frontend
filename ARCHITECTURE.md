# 🏗️ Arquitectura de Deployment

## 📊 Stack Tecnológico

```
┌─────────────────────────────────────────────────────┐
│                   FRONTEND (Vercel)                 │
├─────────────────────────────────────────────────────┤
│  Next.js 15 (App Router)                            │
│  React 19                                           │
│  TypeScript 5.3                                     │
│  Tailwind CSS                                       │
│  Zustand (State Management)                         │
│  React Query (Data Fetching)                        │
└─────────────────────────────────────────────────────┘
                         │
                         │ HTTPS/API Calls
                         ▼
┌─────────────────────────────────────────────────────┐
│              BACKEND (Cloud Run/Railway)            │
├─────────────────────────────────────────────────────┤
│  REST API                                           │
│  Authentication (JWT)                               │
│  Radio Stations Database                            │
│  User Management                                    │
└─────────────────────────────────────────────────────┘
                         │
                         │ Webhook Events
                         ▼
┌─────────────────────────────────────────────────────┐
│                  STRIPE (Payments)                  │
├─────────────────────────────────────────────────────┤
│  Subscription Management                            │
│  Payment Processing                                 │
│  Webhook Events                                     │
└─────────────────────────────────────────────────────┘
```

## 🔄 Flujo de CI/CD

```
┌──────────────┐
│   Developer  │
│    Commit    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│              GitHub Repository                       │
└──────┬───────────────────────────────┬───────────────┘
       │                               │
       │ On PR                         │ On Push to main
       ▼                               ▼
┌─────────────────────┐      ┌─────────────────────────┐
│   GitHub Actions    │      │   GitHub Actions        │
│   (CI Workflow)     │      │   (Deploy Production)   │
├─────────────────────┤      ├─────────────────────────┤
│ ✅ Lint             │      │ ✅ CI Checks            │
│ ✅ Type Check       │      │ ✅ Build                │
│ ✅ Tests            │      │ ✅ Deploy to Vercel     │
│ ✅ Build Validation │      │ ✅ Health Checks        │
│ ✅ Security Audit   │      └────────┬────────────────┘
└─────────┬───────────┘               │
          │                           ▼
          ▼                    ┌──────────────┐
┌─────────────────────┐        │    Vercel    │
│  Preview Deployment │        │  Production  │
│     (Vercel)        │        └──────────────┘
└─────────────────────┘               │
          │                           │
          ▼                           ▼
    Comment on PR              ✅ Live Website
```

## 🌐 Arquitectura de Vercel

```
┌─────────────────────────────────────────────────────────┐
│              Vercel Edge Network (CDN)                  │
│                    (Global Cache)                       │
└────────────┬────────────────────────────────────────────┘
             │
             ├─── /static/* ────► Cached 1 year
             │
             ├─── /sitemap.xml ─► ISR (revalidate 1h)
             │
             ├─── /robots.txt ──► Cached 1 day
             │
             ├─── /[locale] ────► ISR (revalidate varies)
             │
             └─── /api/* ───────► Serverless Functions
                                    (10s timeout)
                                    (1024MB RAM)
```

## 📂 Estructura del Proyecto

```
radio-front/
├── 📁 .github/
│   └── workflows/
│       ├── ci.yml                  # CI pipeline
│       ├── deploy-preview.yml      # Preview deploys
│       └── deploy-production.yml   # Production deploys
│
├── 📁 apps/
│   ├── next/                       # Next.js app (WEB)
│   │   ├── app/                    # App Router
│   │   │   ├── [locale]/          # i18n routes
│   │   │   ├── api/                # API routes
│   │   │   │   └── webhooks/
│   │   │   │       └── stripe/     # Stripe webhooks
│   │   │   ├── layout.tsx
│   │   │   ├── sitemap.ts          # Dynamic sitemap
│   │   │   └── robots.ts
│   │   ├── components/             # React components
│   │   ├── hooks/                  # Custom hooks
│   │   ├── lib/                    # Utilities
│   │   ├── middleware.ts           # Next.js middleware
│   │   ├── next.config.js          # Next.js config ⭐
│   │   └── .env.production.example # Env vars template
│   │
│   └── expo/                       # Mobile app (FUTURE)
│
├── 📁 packages/
│   └── app/                        # Shared code
│       ├── components/             # Shared components
│       ├── domain/                 # Business logic
│       ├── application/            # Use cases
│       └── infrastructure/         # External integrations
│
├── 📁 scripts/
│   ├── vercel-check.js             # Pre-deployment validation ⭐
│   └── analyze-bundle.js           # Bundle size analyzer ⭐
│
├── 📄 vercel.json                  # Vercel configuration ⭐
├── 📄 .vercelignore                # Files to ignore
├── 📄 turbo.json                   # Turborepo config
├── 📄 package.json                 # Root dependencies
│
└── 📚 Documentation/
    ├── DEPLOYMENT.md               # Full deployment guide ⭐
    ├── GITHUB_ACTIONS.md           # CI/CD guide ⭐
    ├── DEPLOYMENT_CHECKLIST.md     # Quick checklist ⭐
    ├── FREE_TIER_OPTIMIZATION.md   # Optimization tips ⭐
    ├── USEFUL_COMMANDS.md          # Command reference ⭐
    ├── README_DEPLOYMENT.md        # Executive summary ⭐
    └── ARCHITECTURE.md             # This file ⭐

⭐ = Archivos creados para deployment
```

## 🔐 Flujo de Seguridad

```
┌────────────────┐
│   Browser      │
└────────┬───────┘
         │ HTTPS (forced)
         ▼
┌─────────────────────────────────┐
│   Vercel Edge Network           │
│   ├── Security Headers          │
│   ├── CSP Headers               │
│   └── Rate Limiting             │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   Next.js Middleware            │
│   ├── CSRF Protection           │
│   ├── Session Validation        │
│   └── Auth Guard                │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   API Routes / Pages            │
│   ├── JWT Validation            │
│   ├── Input Sanitization        │
│   └── Error Handling            │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   Backend API                   │
│   ├── Authentication            │
│   ├── Authorization             │
│   └── Data Validation           │
└─────────────────────────────────┘
```

## 💳 Flujo de Pagos (Stripe)

```
User clicks "Subscribe"
         │
         ▼
┌────────────────────────┐
│  Next.js App           │
│  /api/checkout         │
│  Creates checkout      │
│  session               │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│  Stripe Checkout       │
│  (Hosted page)         │
│  User pays             │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│  Stripe Webhook        │
│  Sends event to:       │
│  /api/webhooks/stripe  │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│  Webhook Handler       │
│  ✅ Verify signature   │
│  ✅ Process event      │
│  ✅ Update backend     │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│  Backend API           │
│  Updates user's        │
│  premium status        │
└────────────────────────┘
```

## 🗺️ Rutas y Caching Strategy

```
Route                    Type        Revalidate    Cache
──────────────────────────────────────────────────────────
/                        SSR         -             Edge 60s
/[locale]                SSR         -             Edge 60s
/[locale]/radio/[id]     ISR         3600s (1h)    Edge + ISR
/[locale]/country/[code] ISR         86400s (24h)  Edge + ISR
/[locale]/genre/[tag]    ISR         86400s (24h)  Edge + ISR
/sitemap.xml             Dynamic     3600s (1h)    Edge 1h
/robots.txt              Static      -             Edge 1d
/api/*                   Serverless  60s           Edge 60s
/_next/static/*          Static      -             Edge 1y
```

## 📊 Data Flow

```
┌──────────┐      ┌──────────┐      ┌──────────┐
│          │      │          │      │          │
│  Client  │◄────►│  Next.js │◄────►│  Backend │
│ (Browser)│      │  (Vercel)│      │   API    │
│          │      │          │      │          │
└──────────┘      └────┬─────┘      └──────────┘
                       │
                       ├──► Stripe (payments)
                       │
                       ├──► Radio APIs (stream URLs)
                       │
                       └──► CDN (images, assets)
```

## 🌍 Deployment Regions

```
Primary Region: US East (iad1)
└── Closest to:
    ├── North America
    ├── Europe (acceptable latency)
    └── South America (acceptable latency)

Future Considerations:
├── Add EU region for European users
├── Add Asia region for Asian users
└── Multi-region requires Vercel Pro plan
```

## 🔄 Cache Hierarchy

```
Level 1: Browser Cache
         └── Static assets (1 year)
         └── Images (varies)

Level 2: Vercel Edge Cache (CDN)
         └── Static pages (ISR)
         └── API responses (60s)
         └── Sitemap (1h)

Level 3: Backend Cache
         └── Database queries
         └── External API calls
         └── Computed data
```

## 📈 Performance Optimizations

```
┌─────────────────────────────────────────┐
│  Image Optimization                     │
│  ├── AVIF format (50% smaller)         │
│  ├── WebP fallback                     │
│  ├── Lazy loading                      │
│  └── Responsive sizes                  │
├─────────────────────────────────────────┤
│  Code Splitting                         │
│  ├── Dynamic imports                   │
│  ├── Route-based splitting             │
│  └── Component-level splitting         │
├─────────────────────────────────────────┤
│  Compression                            │
│  ├── Gzip (text, html, css, js)       │
│  ├── Brotli (automatic in Vercel)     │
│  └── Minification (SWC)               │
├─────────────────────────────────────────┤
│  Caching Strategy                       │
│  ├── Edge caching (CDN)                │
│  ├── ISR (Incremental Static)         │
│  ├── Browser caching                   │
│  └── Stale-while-revalidate           │
└─────────────────────────────────────────┘
```

## 🎯 Límites y Escalabilidad

```
Free Tier Limits (Current):
├── Bandwidth: 100 GB/month
│   └── Estimated usage: 5-10 GB/month (5-10%)
├── Serverless: 100 GB-Hrs/month
│   └── Estimated usage: 10-20 GB-Hrs/month (10-20%)
├── Builds: 6,000 minutes/month
│   └── Estimated usage: 50-100 min/month (~2%)
└── Functions: 10s timeout
    └── All optimized to <10s ✅

Scaling Strategy:
├── 0-3K users/month → Free tier ✅
├── 3K-10K users/month → Monitor closely ⚠️
├── 10K+ users/month → Upgrade to Pro ($20/month)
└── 50K+ users/month → Enterprise plan
```

## 🔍 Monitoring Points

```
┌─────────────────────────────────────────┐
│  Vercel Dashboard                       │
│  ├── Bandwidth usage                   │
│  ├── Function invocations              │
│  ├── Build minutes                     │
│  ├── Error rates                       │
│  └── Response times                    │
├─────────────────────────────────────────┤
│  GitHub Actions                         │
│  ├── Workflow success rate             │
│  ├── Build times                       │
│  ├── Test coverage                     │
│  └── Deployment frequency              │
├─────────────────────────────────────────┤
│  Stripe Dashboard                       │
│  ├── Webhook delivery                  │
│  ├── Payment success rate              │
│  ├── Subscription metrics              │
│  └── Revenue tracking                  │
└─────────────────────────────────────────┘
```

---

**Última actualización**: Enero 2026  
**Versión**: 1.0  
**Estado**: Production Ready ✅
